#include <iostream>
#include <cmath>
using namespace std;

void update(int *a,int *b){
    cout<<*a+*b<<endl;
    if (*a<*b)
        cout<<abs((*a-*b));
    if (*a>*b)
        cout<<*a-*b;

}

int main() {
    int a, b;
    int *pa = &a, *pb = &b;
    cin >> a >> b;
    update(pa, pb);
}
