#include <iostream>

using namespace std;

int main() {
    int n;
    cout << "Ingrese el tamaño:";

    cin >> n;

    int arreglo[n];

    for (int i = 0; i < n; i++)
        arreglo[i] = rand();
    for (int j = 0; j < n; j++)
        cout << "(" << arreglo[j] << ")";
    return 0;
}
